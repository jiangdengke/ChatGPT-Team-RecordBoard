services:
  app:
    build: .
    container_name: chatgpt-team-board
    restart: always
    network_mode: "host"
    environment:
      - NODE_ENV=production
      - DB_HOST=127.0.0.1
      - DB_USER=root
      - DB_PASSWORD=123456
      - DB_NAME=record_board
      - PORT=5000