services:
  app:
    build: .
    container_name: chatgpt-team-board
    restart: always
    ports:
      - "5000:5000"
    volumes:
      - ./data.json:/app/data.json
    environment:
      - NODE_ENV=production
